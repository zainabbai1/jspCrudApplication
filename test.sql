create database test;
use test;

create table register(id int primary key auto_increment,name varchar(255),password varchar(255),email varchar(255),sex varchar(255),country varchar(255));
select * from register;